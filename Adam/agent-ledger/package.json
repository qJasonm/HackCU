{
  "name": "agent-ledger",
  "version": "1.0.0",
  "private": true,
  "description": "A standalone framework for persistent multi-agent coordination & long-term memory",
  "scripts": {
    "build": "pnpm -r build",
    "dev": "pnpm --filter @agent-ledger/mcp-server dev",
    "test": "pnpm -r test",
    "clean": "pnpm -r exec rm -rf dist"
  },
  "devDependencies": {
    "typescript": "^5.4.0",
    "tsx": "^4.7.0",
    "vitest": "^1.6.0",
    "@types/node": "^20.11.0"
  }
}
